## What does this change?

## How was it tested?

- [ ] `uv run pytest`
- [ ] `uvx ruff check backend tests` and `uvx ruff format backend tests`
- [ ] `npm run build` in `frontend/`
- [ ] UI checked in light and dark mode, at phone and desktop widths (if relevant)
